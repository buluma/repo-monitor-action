name: Build and Test

on:
  push:
    branches:
      - master
      - testing
  workflow_dispatch:

jobs:
  BuildAndTest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Build and Test
        run: |
          yarn
          yarn build
          yarn test
